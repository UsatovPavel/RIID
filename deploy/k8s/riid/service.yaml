apiVersion: v1
kind: Service
metadata:
  name: riid-metrics
  namespace: riid-system
  labels:
    app.kubernetes.io/name: riid
spec:
  selector:
    app.kubernetes.io/name: riid
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
