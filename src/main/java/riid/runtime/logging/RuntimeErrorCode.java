package riid.runtime.logging;

/**
 * Stable runtime error codes for structured logging.
 */
public enum RuntimeErrorCode {
    PROCESS_EXIT_NON_ZERO,
    OUTPUT_LIMIT_EXCEEDED,
    PROCESS_IO_ERROR,
    PROCESS_INTERRUPTED
}
